# 핵심 (분석 · 도플러 DSP)
numpy>=1.24
scipy>=1.10

# 모델 학습 / 내보내기 (방식 B, 검출 모델용)
torch>=2.1
# torchaudio>=2.1        # 오디오 증강 (선택)
# onnx>=1.15             # ONNX 내보내기
# onnxruntime>=1.17      # 추론 (Jetson은 TensorRT EP 권장)
# scikit-learn>=1.3      # split / metrics
